# Baked into the client bundle at build time — not secret, since it's a
# public API endpoint visible in the deployed JS anyway. Used by
# `npm run build` (and any CI/Cloudflare Pages build) automatically.
VITE_API_BASE_URL=https://api.chantkaro.com
